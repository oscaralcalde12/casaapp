import { defineConfig } from "@neon/config/v1";

// Plano de Obra usa Lakebase Postgres. No requiere Auth, Data API,
// Functions, AI Gateway ni Object Storage en esta versión.
export default defineConfig({});
